# Dependencies and build output — rebuilt inside the container
node_modules/
dist/

# Local environment secrets
.env.local

# OS files
.DS_Store

# The Dockerfile itself does not need to be copied into the image
Dockerfile
