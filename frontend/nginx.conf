server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA routing — send all paths to index.html so React Router handles them.
    # Without this, navigating directly to /calendar or /drivers returns nginx 404.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
